# Bundled skills — improvement backlog (`lesson-2.9-qa-tests-agent`)

| Folder | Status |
|--------|--------|
| `_to-improve/test-cases` | **Applied (course patch):** Given/When/Then; Vitest/RTL sketch; Playwright IDs; optional fuzz note. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
