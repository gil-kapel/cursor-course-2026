# שיעור 1.4 — על המסך (הוראות צילום)

סיור מודרך בממשק — Agent, בורר המצבים, בורר המודל, Cursor Settings, ו־`Cmd+K`. אין slides. שיעור זה הוא **צילום מסך חי לחלוטין** — כל ביט הוא הדגמה פעילה בתוך Cursor.

טיימקודים ליעדי עריכה; לכוונן אחרי picture lock.

---

## הכנה לפני ההקלטה

**צרו קובץ `index.html` בתוך `My-First-Project` עם התוכן הבא:**

```html
<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>My Project</title>
</head>
<body>
  <h1>כותרת ראשית</h1>
  <p>פסקה לדוגמה</p>
  <button>לחץ עלי</button>
</body>
</html>
```

קובץ זה הוא ה"בד" לכל ההדגמות בשיעור. הכניסו אותו לפרויקט, פתחו אותו ב-Editor, ועשו **git commit** לפני ההקלטה.

---

## 00:00 – 00:45 · פתיחה

**מה על המסך:** Cursor פתוח על `My-First-Project`. `index.html` פתוח ב-Editor מרכזי. Explorer משמאל, Editor באמצע, פאנל הסוכן סגור.

**פעולה:** העכבר נח על שלושת אזורי הממשק לפי הסדר — Explorer, Editor, אזור הפאנל הסגור — תנועה עדינה ואיטית שמציגה את "המפה".

**Overlay (עריכה):**

- `Explorer` — "קבצי הפרויקט"
- `Editor` — "כאן הקוד"
- `Agent / Panel` — "כאן הסוכן"

---

## 00:45 – 02:00 · Agent — איפה רוב העבודה קורית

**Overlay:**

```text
Cmd+I / Ctrl+I
Agent
```

**פעולה:**

1. לחיצת `Cmd+I` במק או `Ctrl+I` בווינדוס/לינוקס, או לחיצה על כפתור Agent בממשק אם זה מה שקיים בגרסה.
2. פאנל הסוכן נפתח.
3. **זום** על שורת הקלט.
4. הקלדה קצרה: `תסביר לי בקצרה מה יש כרגע בקובץ הזה`
5. Enter. תשובה קצרה מופיעה.

---

## 02:00 – 03:00 · בורר המצבים — באיזה מצב אני עובד

**שלב 1 — פתיחת בורר המצבים:**
לחיצה על בורר המצבים בתחתית פאנל הסוכן.

**שלב 2 — זום על הרשימה:**
להראות בבירור את `Ask`, `Agent`, `Plan`, `Debug` אם כל הארבעה קיימים בגרסה.

**שלב 3 — בחירה וחזרה:**
בחירה ב־`Ask` ואז חזרה ל־`Agent`, רק כדי להראות שמדובר בהחלטת מצב עבודה.

**Overlay קצר:** "לפני כל משימה: בדקו באיזה מצב אתם נמצאים"

---

## 03:00 – 04:00 · בורר המודל — איזה מוח עובד עכשיו

**Overlay:**

```text
Model Picker
```

**שלב 1 — פתיחת בורר המודל:**
לחיצה על בורר המודל אם הוא גלוי ליד הסוכן או באזור העבודה.

**שלב 2 — זום על הרשימה:**
להראות 2–3 אפשרויות לפי מה שקיים בגרסה בזמן ההקלטה.

**Overlay נוסף:**

```text
מהיר → יום־יום
חזק → משימות מורכבות
```

**שלב 3 — סגירה:**
לא חייבים להחליף בפועל. אפשר לפתוח, להראות, ולסגור.

---

## 04:00 – 05:15 · Cursor Settings — איפה מכוונים את הסביבה

**Overlay:**

```text
Cmd+, / Ctrl+,
Cursor Settings
```

**שלב 1 — פתיחת ההגדרות:**
`Cmd+,` במק או `Ctrl+,` בווינדוס/לינוקס.

**שלב 2 — סריקה מהירה:**
העכבר עובר לאט על `Plan & Usage`, `Models`, `Agents`, `Indexing & Docs`.

**שלב 3 — סגירה:**
סוגרים את ההגדרות וחוזרים ל־Editor.

---

## 05:15 – 06:15 · Cmd+K — שינוי נקודתי

**מסך:** `index.html` פתוח, הסמן על שורת ה־`<h1>`.

**Overlay:**

```text
Cmd+K / Ctrl+K
שינוי קטן וממוקד
```

**פעולה:**

1. לחיצה על `Cmd+K` במק או `Ctrl+K` בווינדוס/לינוקס.
2. נפתחת שורת קלט קטנה בתוך הקוד.
3. הקלדה: `שנה את הכותרת ל"הפרויקט הראשון שלי"`
4. Enter.
5. **זום** על ה־diff.
6. לחיצה על `Accept`.

---

## 06:15 – 07:00 · מפת החלטה מהירה

**Overlay — טבלת ה-"איפה ומה"** (מופיעה 8 שניות):

| צריך... | פותחים... |
|---------|-----------|
| לעבוד עם ה-AI | **Agent** |
| לבדוק מצב עבודה | **בורר המצבים** |
| להחליט על מודל | **בורר המודל** |
| לכוון את הסביבה | **Cursor Settings** |
| לשנות שורה | **Cmd+K** |

**בזמן שה-overlay מוצג:** העכבר עובר לאט בין אותם אזורים בממשק.

---

## 07:00 – 08:00 · סיכום וטיזר לשיעור 1.5

**מה על המסך:** Cursor פתוח. Editor עם `index.html`. פאנל הסוכן סגור — מסך נקי.

**Overlay checklist** (3 שניות, נעלם):

- ✅ Agent
- ✅ בורר המצבים
- ✅ בורר המודל
- ✅ Cursor Settings
- ✅ Cmd+K

**טיזר ויזואלי ל-1.5:**
שיחת צ'אט ארוכה מאוד — גלילה מהירה מעלה ומטה בשיחה ארוכה. ה"גלילה האינסופית" מייצגת את שאלת הקונטקסט שנעסוק בה ב-1.5.

**Overlay** (2 שניות): "שיעור 1.5 — הזיכרון של הסוכן"

---

## הערות הפקה כלליות

- **אין slides** בשיעור הזה — צילום מסך חי בלבד לאורך כל הדרך.
- **index.html מוכן מראש** — ראו הכנה בתחילת המסמך. אל תיצרו אותו בשידור חי בשיעור.
- **שמות ממשק:** השתמשו בשם שמופיע בגרסה שלכם בפועל.
- **Agent / בורר המודל / בורר המצבים** עשויים להיראות מעט שונה בין גרסאות. צלמו את מה שקיים באמת, לא מה שהיה בגרסה ישנה.
- **פרטיות:** אין מידע אישי בשיעור — אין צורך בטשטוש.
- עשו **git commit** בסוף ההקלטה (מחוץ לצילום) אם נוספו שינויים לפרויקט הדמו.
