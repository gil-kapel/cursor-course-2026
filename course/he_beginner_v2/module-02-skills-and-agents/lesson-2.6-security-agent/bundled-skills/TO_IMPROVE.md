# Bundled skills — improvement backlog (`lesson-2.6-security-agent`)

| Folder | Status |
|--------|--------|
| `_to-improve/threat-modeling-expert` | **Applied (course patch):** STRIDE one-question table; pointers to bundled OpenAI/davila7 threat-model skills. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
