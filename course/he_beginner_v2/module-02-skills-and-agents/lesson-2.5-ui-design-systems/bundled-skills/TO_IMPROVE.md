# Bundled skills — improvement backlog (`lesson-2.5-ui-design-systems`)

| Folder | Status |
|--------|--------|
| `_to-improve/tailwind-best-practices` | **Applied (course patch):** “Course generalization” path mapping (Mastra → generic repo); read order vs `tailwind-design-system`; updated frontmatter. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
