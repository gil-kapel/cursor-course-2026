# Bundled skills — improvement backlog (`lesson-2.1-skills-intro`)

Folders under `bundled-skills/_to-improve/` scored **≤ 80** in `AGENT_SKILLS.md` but are **easy to upgrade** for this course (small edits, no re-architecture).

| Folder | Status |
|--------|--------|
| `_to-improve/creating-cursor-rules-skill` | **Applied (course patch):** 5-line load triggers, Cursor 2.x table (`.cursor/rules` vs `.cursorrules`), Next.js-style `.mdc` worked example. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
