# שיעור 2.11 — על המסך (הוראות צילום)

טיימקודים ליעדי עריכה; לכוונן אחרי picture lock.

---

## 00:00 – 01:30

צ'אט עם `local-db-development-agent/SKILL.md`. לצדו — קובץ `architecture-handoff.md` או פתק קצר על דרישות נתונים לפרויקט הדגמה.

---

## 01:30 – 04:00

בקשה: "השווה SQLite מול JSON לפרויקט הזה בשלב הפיתוח; המלץ ותן סכימה ראשונית". גלילה בתשובה.

---

## 04:00 – 06:00

אם יש בפרויקט `*.db` או `data.json` — פתיחה בviewer או בעורך (טקסט קצר בלבד). אם לא — יצירת `dev-data/` והדגמת קובץ `seed.json` קטן עם מערך משימות (2–3 אובייקטים).

---

## 06:00 – 07:30

טרמינל: פקודה לא משנה איזו — `ls dev-data` או הצגת עץ תיקיות; בלי סיסמאות או URI אמיתיים של בסיס נתונים.

---

## 07:30 – 08:30

סיום: `README` של השיעור + רשימת קבצים במודול 2 ב־Explorer; משפט קצר בצ'אט "סיכום מודול 2 — צוות סוכנים מהתכנון ועד נתונים מקומיים".
