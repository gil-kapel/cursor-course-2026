# שיעור 1.7 — על המסך (הוראות צילום)

הגדרות קורסור. שיעור זה הוא **צילום מסך חי לחלוטין** — אין slides.

טיימקודים ליעדי עריכה; לכוונן אחרי picture lock.

---

## הכנה לפני ההקלטה

Cursor פתוח על My-First-Project. קבצים מהשיעורים הקודמים קיימים: `index.html`, `style.css`, `contact.html`. Git committed. פאנל הצ'אט סגור.

**וידוא:**

- חלון ההגדרות נגיש דרך Cmd+, או דרך תפריט Cursor
- אזור Indexing & Docs ריק מתיעודים מותאמים (כדי להדגים הוספה חיה)
- יש חיבור אינטרנט פעיל (נדרש להורדת תיעוד)
- מוכנים כתובות URL לדוגמה: `https://tailwindcss.com/docs`, `https://react.dev`

---

## 00:00 – 00:45 · פתיחה

**מה על המסך:** Cursor פתוח. Editor עם קובץ כלשהו. פאנל צ'אט סגור.

**פעולה:** לחיצה על Cmd+, (או Cursor → Settings). חלון ההגדרות נפתח. **זום** על התפריט הצדדי שמציג את כל הסעיפים.

**Overlay** (3 שניות): "שיעור 1.7 — הגדרות קורסור"

---

## 00:45 – 01:45 · General — העדפות בסיסיות

**שלב 1 — Editor Settings:**
לחיצה על "General" בתפריט. **זום** על אזור ה-Preferences. לחיצה על "Editor Settings" → Open. מראים את הגדרות הפונט, הערכת נושא (Theme), והמינימאפ.

**שלב 2 — שינוי לדוגמה:**
שינוי גודל פונט (למשל מ-14 ל-16). **זום** על השינוי ב-Editor ברקע — הפונט גדל.

**שלב 3 — Layout:**
גלילה למטה לאזור Layout. מראים את Toggle של Title Bar ו-Status Bar. לחיצה על אחד מהם כדי להדגים — שורה נעלמת/מופיעה.

**Overlay** (2 שניות): "General = נוחות אישית"

---

## 01:45 – 02:45 · Plan & Usage + Models

**שלב 1 — Plan & Usage:**
לחיצה על "Plan & Usage" בתפריט. **זום** על פרטי המנוי: סוג תוכנית, שימוש, מכסה. **השהייה** 3 שניות — "כאן בודקים כמה נשאר".

**שלב 2 — Models:**
לחיצה על "Models" בתפריט. **זום** על רשימת המודלים הזמינים. מראים את המודל הנבחר כברירת מחדל. גלילה קצרה על שאר המודלים.

**Overlay** (3 שניות):

```text
מודלים:
מהיר → משימות יומיומיות
חזק → חשיבה מורכבת
```

---

## 02:45 – 03:45 · Tab, Agents & Cloud Agents

**שלב 1 — Tab:**
לחיצה על "Tab" בתפריט. **זום** על הגדרות ההשלמה האוטומטית. מראים את האפשרויות — הפעלה/כיבוי, רגישות.

**שלב 2 — Agents:**
לחיצה על "Agents" בתפריט. מראים את הגדרות הסוכן — הרשאות, התנהגות. **זום** קצר.

**שלב 3 — Cloud Agents:**
לחיצה על "Cloud Agents" בתפריט. מראים את המסך — הזכרה קצרה בלבד.

**Overlay** (2 שניות): "Tab + Agents = כוונון עדין של ההשלמה והסוכן"

---

## 03:45 – 05:30 · Indexing & Docs — תיעוד מותאם

**זו ההדגמה המרכזית של השיעור.**

**שלב 1 — מצב האינדוקס:**
לחיצה על "Indexing & Docs" בתפריט. **זום** על סטטוס האינדוקס של הפרויקט — מראים שהפרויקט הנוכחי מאונדקס.

**Overlay** (3 שניות): "Indexing = המפה הפנימית של הפרויקט שלכם"

**שלב 2 — הוספת תיעוד:**
גלילה למטה לאזור ה-Docs. **זום** על האזור הריק. לחיצה על כפתור הוספה / שדה הוספת URL. הקלדה איטית וברורה:

```text
https://tailwindcss.com/docs
```

לחיצה על אישור/Enter. **זום** על התיעוד שנוסף לרשימה. **השהייה** 2 שניות.

**שלב 3 — הוספת תיעוד נוסף:**
הוספת URL נוסף:

```text
https://react.dev
```

אישור. מראים שעכשיו יש שני תיעודים ברשימה.

**Overlay** (4 שניות):

```text
Docs:
1. הדביקו URL של תיעוד
2. קורסור מוריד ושומר
3. ה-AI משתמש בתיעוד העדכני
```

**שלב 4 — הוכחה שזה עובד:**
סגירת ההגדרות. פתיחת צ'אט חדש (Cmd+L). הקלדה: `מה ה-class של Tailwind שעושה flexbox עם רווח בין אלמנטים?`
Enter. **זום** על תשובת ה-AI — שמשתמשת במידע מהתיעוד העדכני.

**Overlay** (2 שניות): "תיעוד חכם = תשובות מדויקות יותר"

---

## 05:30 – 06:00 · Network, Beta, ומה שנדלג

**שלב 1 — Network:**
לחיצה מהירה על "Network" בתפריט. **זום** קצר — מראים שיש הגדרות proxy. לא נכנסים לעומק.

**שלב 2 — Beta:**
לחיצה על "Beta" בתפריט. **זום** על רשימת הפיצ'רים הניסיוניים. לא משנים כלום.

**שלב 3 — הסעיפים שנדלגים:**
גלילה מהירה על: Plugins, Rules/Skills/Subagents, Tools & MCPs, Hooks. **לא נכנסים** לאף אחד מהם.

**Overlay** (4 שניות):

```text
Plugins → Hooks
⏭️ נלמד בהמשך הקורס
(מודול 2 ומודול 3)
```

---

## 06:00 – 07:00 · Hands-on — הגדרה חיה

**שלב 1 — חזרה להגדרות:**
Cmd+, → חלון ההגדרות נפתח.

**שלב 2 — Editor Settings:**
שינוי הגדרה נוספת לבחירתכם — למשל ערכת נושא. **זום** על השינוי שמתבצע ב-Editor.

**שלב 3 — Indexing & Docs:**
מעבר ל-Indexing & Docs. מראים שהתיעודים שהוספנו קודם עדיין שם. אופציונלי: הוספת תיעוד שלישי (למשל Prisma).

**שלב 4 — בדיקה בצ'אט:**
סגירת הגדרות. פתיחת צ'אט חדש. שאלה הקשורה לתיעוד שהוסף. ה-AI עונה עם מידע מדויק.

**Overlay** (2 שניות): "מוגדר ומוכן לעבודה"

---

## 07:00 – 08:00 · סיכום — מודול 1 הושלם

**מה על המסך:** Cursor פתוח. צ'אט נקי. פרויקט עם מספר קבצים ב-Explorer. Git indicator פעיל.

**Overlay חגיגי — "מודול 1 הושלם"** (6 שניות):

```text
✅ סביבת עבודה — Cursor מותקן ומוגדר
✅ מנוי — מודלים ומכסות
✅ ביטחון — Terminal + Git
✅ כלים — Tab / Cmd+K / Cmd+L / Cmd+I
✅ זיכרון — ניהול קונטקסט
✅ מצבי עבודה — Agent / Ask / Plan / Debug
✅ הגדרות — סביבה מותאמת + תיעוד חכם
```

**Overlay — מסגרת סיכום** (4 שניות):

| מה למדנו | שיעור |
|-----------|-------|
| התקנה ופתיחה | 1.1 |
| מנויים ומודלים | 1.2 |
| טרמינל וגיט | 1.3 |
| כלי עריכה | 1.4 |
| ניהול קונטקסט | 1.5 |
| מצבי עבודה | 1.6 |
| הגדרות ותיעוד | 1.7 |

**טיזר למודול 2:**
**Overlay** (3 שניות): "מודול 2 — Skills ופיתוח מונחה תפקידים"
עם אייקון קטן של כל "סוכן" (🧑‍💼 מנהל מוצר, 🏗 ארכיטקט, 🎨 מעצב, 💻 מפתח).

מסך נקי. סגירה.

---

## הערות הפקה כלליות

- **אין slides** בשיעור הזה — צילום מסך חי בלבד.
- **Indexing & Docs** הוא ההדגמה המרכזית. ודאו שהאזור ריק מתיעודים לפני ההקלטה כדי להראות הוספה חיה.
- **ה-URL-ים לדוגמה** צריכים להיות אתרים שעובדים ונטענים — בדקו לפני ההקלטה ש-`tailwindcss.com/docs` ו-`react.dev` נגישים.
- **הסעיפים שנדלגים** (Plugins → Hooks) — גלילה מהירה עליהם בלבד, לא נכנסים. ה-Overlay מסביר שזה יילמד בהמשך.
- **ה-overlay החגיגי** "מודול 1 הושלם" הוא רגע שיא — אל תמהרו אותו. תנו לו 6 שניות מלאות.
- **הגדרות Editor:** אם שיניתם גודל פונט/ערכת נושא להדגמה, אפשר להשאיר את השינוי או להחזיר — לפי העדפה.
- **פרטיות:** אין מידע אישי בשיעור — אין צורך בטשטוש. ייתכן שמסך Plan & Usage מציג אימייל — טשטשו אם רלוונטי.
- עשו **git commit** בסוף ההקלטה (מחוץ לצילום) — ממשיך את הרגל 1.3.
