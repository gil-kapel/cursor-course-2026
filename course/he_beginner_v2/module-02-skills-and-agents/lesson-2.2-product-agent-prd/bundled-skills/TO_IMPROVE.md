# Bundled skills — improvement backlog (`lesson-2.2-product-agent-prd`)

| Folder | Status |
|--------|--------|
| `_to-improve/doc-prd-aidoc-flow` | **Applied (course patch):** “Course addendum” with SDD vs lean PRD decision table, pointer to `prd-awesome-copilot`, no blocking on missing BRD. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
