# UX Handoff Defaults

Every UX handoff in this course should include:

- user and goal
- main journey
- alternate or failure paths
- screen inventory
- state list per screen: default, loading, empty, error, success
- handoff notes for UI and dev

If there are more than 8 flow steps, split into main path plus secondary paths.
