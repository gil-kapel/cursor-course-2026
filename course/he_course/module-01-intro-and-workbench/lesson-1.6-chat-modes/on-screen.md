# שיעור 1.6 — על המסך (הוראות צילום)

**דוגמה אחת לאורך כל השיעור:** אפליקציית Contact List ב-React + Tailwind — רצף **בנייה ראשונית → Ask → Plan (כולל תיקון תוכנית) → Agent → בדיקה בדפדפן → Debug → Max**. צילום מסך חי בלבד — אין slides.

טיימקודים ליעדי עריכה; לכוונן אחרי picture lock.

---

## הכנה לפני ההקלטה

Cursor פתוח על תיקייה ריקה או פרויקט חדש. פאנל הסוכן פתוח.

**וידוא:**

- בורר המצבים: `Ask`, `Agent`, `Plan`, `Debug` (או מקבילות בגרסה)
- דפדפן מובנה של Cursor זמין
- תסריט התקלה ל-Debug מוכן מראש: שינוי שם class ב-CSS או מחיקת שורה בקומפוננטה — כך שהטופס או הרשימה נשברים

**Overlay מסגרת** (בפתיחה, 4 שניות):

```text
דוגמה אחת: Contact List
Build → Ask → Plan → Agent → דפדפן → Debug
```

---

## 00:00 – 00:55 · פתיחה — בונים פרויקט דוגמה

**מה על המסך:** Cursor. פאנל הסוכן פתוח. מצב **Agent**.

**הקלדה (פרומפט):**

```text
Build a simple, single-page Contact List app using React and Tailwind CSS. It should allow users to add a name and phone number, display them in a list, and delete entries. Keep the state local (no database for now) and focus on a clean, modern UI, and open in cursor built-in browser
```

Enter. מעקב: הסוכן יוצר פרויקט, מתקין חבילות, מציג דף ראשוני.

**Overlay** (4 שניות): "שיעור 1.6 — מצבים דרך דוגמה אמיתית"

---

## 00:55 – 01:40 · סקירה — מה בודקים בכל מצב

**מה על המסך:** הפרויקט שנוצר. בורר מצבים גלוי או נפתח לרגע.

**Overlay** (6 שניות):

| mode | מה בודקים |
|------|-----------|
| **Ask** | בהירות — לא "חכם" |
| **Plan** | תוכנית מלאה — מותר לתקן |
| **Agent** | בקשה ממוקדת |
| **Debug** | סימפטום + ראיות |

---

## 01:40 – 02:55 · Ask — מה אפשר לשפר

**מצב:** `Ask`.

**הקלדה (דוגמה):**

```text
מה אפשר לשפר באפליקציה הזו? מה חסר מבחינת ולידציה, חוויית משתמש, ו-edge cases?
```

Enter. **זום** על התשובה — רשימות והמלצות. **אין** שינוי קבצים.

**Overlay** (3 שניות): "Ask = מכינים את השאלה הנכונה ל-Plan"

---

## 02:55 – 04:25 · Plan — תוכנית ראשונה ואז עדכון תוכנית

**מצב:** `Plan`.

**הודעה ראשונה (דוגמה):**

```text
תן תוכנית מסודרת לשיפור האפליקציה: ולידציה לשדות, feedback למשתמש, עיצוב מותאם.
```

Enter. **זום** על התוכנית — קומפוננטות, צעדים, סדר.

**הודעה שנייה — תיקון תוכנית (חובה בהקלטה):**

```text
עדכן את התוכנית: הוסף טיפול בשדות ריקים עם הודעת שגיאה ויזואלית, ואפשרות undo אחרי מחיקה.
```

Enter. **זום** על התוכנית המעודכנת.

**אישור התוכנית** לפי ממשק Cursor (כפתור אישור / Continue — לפי הגרסה).

**Overlay** (3 שניות): "Plan = אפשר לתקן לפני שבונים"

---

## 04:25 – 05:45 · Agent — מימוש ובדיקה בדפדפן

**מצב:** `Agent`.

**הודעה (דוגמה):**

```text
ממש את התוכנית המעודכנת מלמעלה.
```

Enter. מעקב אחרי שינויים בקוד.

**בדיקה:** פתיחה בדפדפן — הוספת איש קשר, ולידציה עם שדות ריקים, מחיקה עם undo.

**Overlay** (3 שניות): "Agent = עד שזה רץ"

---

## 05:45 – 07:00 · Debug — שוברים ומתקנים

**לפני מעבר ל-Debug:** יצירת תקלה מבוקרת — למשל שינוי שם class ב-CSS או מחיקת שורה בקומפוננטה כך שהטופס או הרשימה נשברים.

**מצב:** `Debug`.

**הודעה (דוגמה):**

```text
הטופס לא מציג את הרשימה אחרי הוספה. תחקור ותתקן.
```

Enter. **זום** על הניתוח והתיקון.

**אימות:** רענון בדפדפן — הכל עובד שוב.

**Overlay** (3 שניות): "Debug = סימפטום + ראיות, לא 'תקן הכל'"

---

## 07:00 – 07:50 · Max mode — קונטקסט, מחיר, מתי לא

**מה על המסך:** אזור המודל / אפשרויות מתקדמות — **Max** או המקבילה בגרסה.

**פעולה:** להראות איפה המתג נמצא. **לא חובה** להפעיל אם זה יקר להקלטה.

**Overlay** (5 שניות):

```text
Max ≈ יותר קונטקסט באותה בקשה
יותר קלט → לרוב יותר עלות (טוקנים)
עדיין אפשר רעש — לא קסם
```

**הערת קריין:** קישור לשיעור 1.2 (תמחור) ו-1.5 (רעש).

---

## 07:50 – 08:25 · סיכום מעבר מצבים וטיזר 1.7

**מה על המסך:** בורר מצבים — מעבר קצר בין שני מצבים.

**Overlay** (5 שניות):

```text
לפני כל שליחה: האם המצב נכון?
Ask → Plan (עדכון) → Agent → דפדפן → Debug
```

**טיזר 1.7:** מרחף על `Cmd+,` / `Ctrl+,` (הגדרות) בלי להיכנס.

סגירה.

---

## הערות הפקה כלליות

- **ליבת השיעור** היא הרצף **Build → Ask → Plan עם תיקון → Agent עם דפדפן → Debug עם אימות**.
- **פרטיות:** אין מידע אישי אמיתי בטפסים.
- **git commit** אחרי ההקלטה (מחוץ לווידאו) — המשך הרגל מ-1.3.
