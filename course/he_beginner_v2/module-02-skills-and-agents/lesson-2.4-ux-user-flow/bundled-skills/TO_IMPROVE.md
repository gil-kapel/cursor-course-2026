# Bundled skills — improvement backlog (`lesson-2.4-ux-user-flow`)

| Folder | Status |
|--------|--------|
| `_to-improve/user-research-flows` | **Applied (course patch):** “Handoff to UX / UI” section with screen list, states, edge cases, example fragment. |
| `_to-improve/ux-flow-diagram` | **Applied (course patch):** When **not** to diagram; Mermaid standard + example; ASCII retained. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
