# Bundled skills — improvement backlog (`lesson-2.10-debug-agent`)

| Folder | Status |
|--------|--------|
| `_to-improve/debug` | **Applied (course patch):** Evidence ritual (traceback + log tail + repro); Composer smallest-diff workflow; updated description. |

Recommended copies (score **≥ 81**, i.e. strictly > 80) live directly under `bundled-skills/<name>/` (not in `_to-improve/`).
