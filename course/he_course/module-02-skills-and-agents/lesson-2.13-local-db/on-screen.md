# שיעור 2.13 — על המסך (הוראות צילום)

טיימקודים ליעדי עריכה; לכוונן אחרי picture lock.

---

## 00:00 – 01:00

Cursor פתוח. בטאבים רואים את `docs/architecture.md`.
העכבר פותח את הצ'אט ומצרף את מסמך הארכיטקטורה.
כיתוב overlay: **Step 9: Local Persistence**.

---

## 01:00 – 02:30

הפעלת סקיל הנתונים: `local-db-development-agent/SKILL.md`.
הדבקת הפרומפט מה־Appendix.
צפייה בסוכן משווה בין SQLite ל־JSON.
זום על המלצת הסוכן (למשל: "JSON is enough for this MVP stage").
הצגת הסכימה המוצעת: `id`, `title`, `status`, `createdAt`.

---

## 02:30 – 03:45

הסוכן יוצר את התיקייה `dev-data` ואת הקובץ `seed.json`.
פתיחת הקובץ החדש והצגת הנתונים.
הדגשת העובדה שהנתונים האלו "מזריקים חיים" לאפליקציה בזמן הפיתוח.

---

## 03:45 – 05:00

הצגת הקוד שקורא את הנתונים (למשל ב־Service).
זום על שימוש ב־Environment Variables (משתני סביבה).
הצגת קובץ `.env.example` ב־Sidebar.

---

## 05:00 – 06:30

סיום המודול: מעבר מהיר על כל הקבצים שנוצרו בתיקיית `docs` ובתיקיות הקוד.
הצגת חלון דפדפן עם האפליקציה הפועלת.
כותרת סיום: **Module 2 Complete — Next: Module 3 (MCP & Live Tools)**.
