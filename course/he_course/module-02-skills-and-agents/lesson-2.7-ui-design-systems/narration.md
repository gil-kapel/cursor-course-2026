# שיעור 2.7 (ElevenLabs / עברית)
---

## 00:00 – 01:00

יש לנו `docs/ux-flows.md` מהשיעור הקודם. עכשיו הסוכן הבא: **UI**.

לפני שמתחילים, מוודאים שהסקיל `ui-design-systems-agent` מותקן ב-`.cursor/skills/`. פותחים את `SKILL.md` ובודקים שהוא שם.

## 01:00 – 02:15

מצרפים `@docs/ux-flows.md` ו-`@docs/architecture.md` ומפעילים את הסוכן.

אני לא מבקש ישר מסמך. אני רוצה שהסוכן ישאל: מה הסגנון הויזואלי? מה חייב להיות בטופס הוספת משימה? איך זה נראה במובייל? מה המשתמש צריך לראות כדי להרגיש שהפעולה הצליחה?

## 02:15 – 03:30

אחרי סבב שאלות, הסוכן מייצר `docs/ui-plan.md`.

המסמך כולל: רשימת קומפוננטות (TaskCard, TaskForm, EmptyState, StatusBadge), design tokens בסיסיים, מצבי ממשק (loading, success, error), נקודות נגישות, והתנהגות responsive.

## 03:30 – 04:45

אופציונלי: אם רוצים כיוון ויזואלי, הסוכן מציע ללכת ל-Dribbble, לבחור השראה, ולהעלות אותה. אחרי שבוחרים reference — הסוכן מחדד את ה-UI plan ומייצר prompt חזק יותר ל-Stitch.

זה תהליך ידני — במודול MCP נלמד איך לחבר את זה אוטומטית.

## 04:45 – 06:00

לסיכום: הסוכן שאל שאלות, בנה תוכנית UI, ושמר `docs/ui-plan.md`. אם השתמשנו ב-Stitch — גם החזרנו חומרים לפרויקט.

בשיעור הבא, 2.8, ניקח את כל המסמכים ונבנה תוכנית מימוש מסודרת ב-Plan mode — לפני שכותבים שורת קוד.
